package views

templ Home(title string, char string, animes [4]string) {
	<div hx-get="/" hx-trigger="click">
		{ char }
		<ul>
			for _, anime := range animes {
				<button>{ anime }</button>
			}
		</ul>
	</div>
}
